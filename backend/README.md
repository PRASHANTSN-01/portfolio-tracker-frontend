# WealthManager Backend

Run locally:
1. cd backend
2. npm install
3. npm start

Deploy on Render:
- Create a new Web Service
- Connect this repo
- Build Command: npm install
- Start Command: npm start
